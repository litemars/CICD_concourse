platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk

#What resources from the pipeline we want as input
inputs:
- name: repo
- nmae: reposource
#Run the script that exists in the resource we requested
run:
  path: repo/script/start.sh
params:
  u: reposource/path.txt
